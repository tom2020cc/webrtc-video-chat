# 🚀 快速域名测试 - 10分钟指南

## ⚡ 3种快速测试方案

### 方案1：使用ngrok (最快速 - 5分钟)

#### 1. 下载并注册ngrok
```bash
# 访问: https://ngrok.com/signup
# 下载Windows版本
# 解压到任意目录
```

#### 2. 获取authtoken
```bash
# 在ngrok网站复制authtoken
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

#### 3. 启动隧道
```bash
# 在项目目录运行
ngrok http 3000

# 会显示类似信息：
# Forwarding: https://abc123.ngrok.io -> http://localhost:3000
```

#### 4. 测试访问
```bash
# 手机浏览器访问:
https://abc123.ngrok.io

# 电脑浏览器访问:
https://abc123.ngrok.io
```

**优点**: ✅ 即时可用，无需域名配置
**缺点**: ❌ 免费版有连接限制，域名不固定

---

### 方案2：使用花生壳 (中文用户 - 10分钟)

#### 1. 注册花生壳
```bash
# 访问: https://hsk.oray.com/
# 注册免费账户
```

#### 2. 下载客户端
```bash
# 下载花生壳客户端
# 安装并登录
```

#### 3. 配置内网映射
```bash
# 添加映射:
# 本地端口: 3000
# 选择免费域名 (如: xxx.xxx.icu)
```

#### 4. 测试访问
```bash
# 获得域名: your-domain.oray.com
# 手机浏览器访问: http://your-domain.oray.com
```

**优点**: ✅ 中文界面，免费域名固定
**缺点**: ❌ 免费版带宽限制

---

### 方案3：云服务器部署 (专业方案 - 30分钟)

#### 1. 购买云服务器 (推荐)
```bash
# 阿里云ECS: https://www.aliyun.com/product/ecs
# 腾讯云CVM: https://cloud.tencent.com/product/cvm
# 选择最低配置即可: 1核1G, 1Mbps
# 成本: 约 ¥50-100/月
```

#### 2. 域名配置
```bash
# 购买域名 (可选，也可使用免费域名)
# 配置DNS解析:
# A记录: your-domain.com -> 服务器IP
```

#### 3. 服务器环境搭建
```bash
# 连接服务器后执行:
# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 上传项目文件
git clone <your-repo-url>
cd webrtc-video-chat
npm install

# 启动服务
npm start
```

**优点**: ✅ 稳定可靠，性能好，自定义域名
**缺点**: ❌ 需要一定技术能力，有费用

---

## 🎯 推荐测试流程

### 第一步：快速验证 (使用ngrok)
```bash
# 1. 下载ngrok并启动
ngrok http 3000

# 2. 手机浏览器访问显示的域名
# 3. 测试基本功能是否正常
# 4. 验证摄像头权限是否可以获取
```

### 第二步：稳定测试 (购买云服务器)
```bash
# 1. 购买云服务器 (最低配置即可)
# 2. 部署项目 (参考详细部署指南)
# 3. 配置域名 (可选)
# 4. 进行完整功能测试
```

### 第三步：生产部署 (配置HTTPS)
```bash
# 1. 申请免费SSL证书 (Let's Encrypt)
# 2. 配置Nginx反向代理
# 3. 正式对外提供服务
```

---

## 🔥 立即开始

### 选择适合您的方案：

**💨 想立即测试** → 使用ngrok (5分钟)
**🏠 中文用户** → 使用花生壳 (10分钟)
**🚀 长期使用** → 购买云服务器 (30分钟)

### 当前项目状态：
```bash
✅ HTTP服务器已启动 (端口3000)
✅ 支持真实域名访问
✅ 配置了自动服务器连接检测
📱 手机访问: http://192.168.31.155:3000 (局域网)
🌐 域名访问: 需要配置上述方案之一
```

---

## 📱 测试清单

完成域名配置后，请测试以下功能：

### 基础功能
- [ ] 页面正常加载
- [ ] 不显示"无法连接服务器"
- [ ] 用户信息正确显示 (PeerID, 设备ID等)
- [ ] 可以创建和加入房间

### 摄像头/麦克风 (HTTP环境可能受限)
- [ ] 能获取摄像头权限
- [ ] 能获取麦克风权限
- [ ] 视频画面正常显示
- [ ] 音频正常收音

### 高级功能
- [ ] 屏幕共享
- [ ] 文件传输
- [ ] AI字幕功能
- [ ] 聊天功能

### HTTPS环境 (如果配置)
- [ ] 摄像头权限正常
- [ ] 麦克风权限正常
- [ ] 浏览器不显示安全警告

---

## 🆘 常见问题

### Q: ngrok显示的域名每次都变？
A: 免费版域名会变化，付费版($5/月)可固定域名。

### Q: HTTP模式下摄像头无法使用？
A: 浏览器安全限制，建议配置HTTPS或使用ngrok的HTTPS隧道。

### Q: 云服务器选择哪个？
A: 国内选择阿里云/腾讯云，国际选择AWS/DigitalOcean。

### Q: 免费域名哪里获取？
A: Freenom (.tk, .ml等) 或 花生壳免费域名。

---

## 📞 下一步

**选择方案后，请参考**：
- ngrok用户: 直接运行 `ngrok http 3000`
- 花生壳用户: 下载客户端配置内网映射
- 云服务器用户: 查看 `真实域名部署指南.md`

现在您可以选择最适合的方案开始真实环境测试了！🎉