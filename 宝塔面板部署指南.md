# 🟢 宝塔面板部署指南

本指南面向 **宝塔面板（Linux）+ Nginx** 环境，把本项目部署到一个已有多个网站的宝塔服务器上，与其他网站共存。
全程只需占用服务器内部端口 **3000**，公网只开放 80/443，**不会影响其他网站**。

> 本项目为**单端口架构**：网页、Socket.IO 信令、PeerJS 信令（`/peerjs` 路径）全部走 3000，
> Nginx 只需反代一个端口、一个 location，无需额外开信令端口。

---

## ✅ 部署前检查

| 检查项 | 要求 | 说明 |
|--------|------|------|
| 宝塔面板 | 7.x / 8.x / 9.x 均可 | |
| Nginx | 任意 1.18+ 版本 | 反代 WebSocket 用 |
| Node.js | >= 16（推荐 18 LTS） | 通过"Node.js版本管理器"安装 |
| 端口 3000 | 服务器内部空闲 | 只在内网监听，**无需**在安全组放行 |
| 域名 | 一个子域名如 `chat.your-domain.com` | 摄像头功能必须 HTTPS |

> 💡 **为什么不影响其他网站**：视频/音频是 P2P 直连（浏览器之间），服务器只转发信令和聊天文字，
> 带宽占用极小；3000 端口只监听内网反代，不与任何现有站点冲突。

---

## 📦 第一步：安装 Node.js

1. 宝塔面板 → **软件商店** → 搜索 **Node.js版本管理器** → 安装
2. 打开 Node.js版本管理器 → 安装 **v18**（勾选"设为默认版本"）

如果软件商店没有该插件，也可以用 SSH 终端手动安装 Node 后跳过此步（宝塔的 Node 项目功能会自动找到 node）。

---

## 📁 第二步：上传代码

方式一（推荐，SSH 终端）：
```bash
cd /www/wwwroot
git clone https://github.com/tom2020cc/webrtc-video-chat.git
cd webrtc-video-chat
npm install --registry=https://registry.npmmirror.com
```

方式二（面板操作）：
1. 本地下载项目 ZIP → 宝塔 **文件** → 进入 `/www/wwwroot` → 上传并解压
2. 宝塔 **终端** 里执行：
```bash
cd /www/wwwroot/webrtc-video-chat
npm install --registry=https://registry.npmmirror.com
```

---

## 🚀 第三步：启动服务（PM2 守护）

### 方式一：宝塔 Node 项目（推荐）

宝塔面板 → **网站** → **Node项目** → **添加Node项目**：

| 配置项 | 值 |
|--------|-----|
| 项目目录 | `/www/wwwroot/webrtc-video-chat` |
| 启动选项 | `start`（即 `node server/app.js`） |
| Node版本 | 18 |
| 项目端口 | `3000` |
| 映射域名 | 留空（我们用 Nginx 站点反代） |
| 是否ssl | 留空 |

保存后宝塔会自动用 PM2 拉起进程并开机自启。

### 方式二：SSH 手动 PM2

```bash
npm install -g pm2
cd /www/wwwroot/webrtc-video-chat
pm2 start server/app.js --name webrtc-app
pm2 save && pm2 startup
```

验证：
```bash
curl http://127.0.0.1:3000        # 返回 HTML 页面
pm2 logs webrtc-app               # 看到 "服务器启动，监听端口 3000"
```

---

## 🌐 第四步：Nginx 反向代理

### 1. 创建站点

宝塔 → **网站** → **添加站点**：
- 域名：`chat.your-domain.com`
- 根目录：随意（如 `/www/wwwroot/chat-proxy`，纯反代不落盘）
- PHP版本：**纯静态**

### 2. 添加反向代理

网站 → `chat.your-domain.com` → **设置** → **反向代理** → **添加反向代理**：
- 代理名称：`webrtc`
- 目标URL：`http://127.0.0.1:3000`
- 发送域名：`$host`

### 3. 检查代理配置（关键）

添加完成后，仍在 **反向代理** 页面点击该条目的 **配置文件**，确认内容包含 WebSocket 升级头和足够长的超时（宝塔新版本模板默认带 Upgrade 头，但超时一般需要调大）：

```nginx
location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;      # 必须：WebSocket 升级
    proxy_set_header Connection "upgrade";       # 必须：WebSocket 升级
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 300s;                     # 建议：长连接心跳容错
    proxy_send_timeout 300s;
}
```

> ⚠️ 缺少 `Upgrade` / `Connection` 两行是最常见故障：网页能打开但**进房间后视频/聊天无反应**（WebSocket 握手失败）。

---

## 🔒 第五步：申请 SSL 证书（必须）

浏览器只在 **HTTPS**（或 localhost）下允许调用摄像头/麦克风，公网部署必须上证书：

1. 网站 → `chat.your-domain.com` → **设置** → **SSL**
2. 选 **Let's Encrypt** → 勾选域名 → **申请**（确保域名已解析到本服务器）
3. 开启 **强制HTTPS**

本项目客户端会自动跟随页面协议（HTTPS 页面自动用 wss/https 连接信令），无需额外配置。

---

## 🛡️ 第六步：端口与安全

### 防火墙/安全组

- 公网只需 **80 / 443**（宝塔默认已放行）
- **3000 不要放行到公网**：只让 Nginx 内网反代访问，多站点环境更安全
- 宝塔 SSH 默认端口保持不变

### 部署后必改的两处安全配置

编辑 `server/app.js`：

1. **管理员密码**（默认 `admin123`，必须改）：
```js
const ADMIN_CONFIG = {
  password: "改成你自己的强密码",
  // ...
};
```

2. **管理员 IP 白名单**（默认只允许本机登录，服务器部署后你在自己电脑上登录会被拒绝）：
```js
allowedIPs: ["*"],   // 或只加你自己的公网IP，如 ["127.0.0.1", "1.2.3.4"]
```

改完执行 `pm2 restart webrtc-app` 生效。

> CORS 当前为 `*`（任意来源），个人测试没问题；如果同一台服务器上有敏感站点，建议把
> `socketIo(server, { cors: ... })` 的 `origin` 改为你的域名，如 `["https://chat.your-domain.com"]`。

---

## 🧪 第七步：部署验证清单

按顺序验证（全过即部署成功）：

| # | 验证项 | 方法 | 预期 |
|---|--------|------|------|
| 1 | 页面可访问 | 浏览器打开 `https://chat.your-domain.com` | 出现首页，地址栏有锁 |
| 2 | 信令通道 | F12 → Network → WS | `socket.io` 与 `peerjs` 两条 WS 均为 101 状态 |
| 3 | 创建/加入房间 | 两个设备（如电脑+手机）进同一房间 | 都能看到对方视频 |
| 4 | 摄像头 | 允许浏览器权限 | 本地画面出现 |
| 5 | 聊天/图片 | 发文字和图片 | 对方实时收到 |
| 6 | 文件传输 | 发送一个大文件 | 双方显示真实进度，完成后可保存 |
| 7 | 数据持久化 | 重启 `pm2 restart webrtc-app` 后刷新 | 聊天记录还在（1小时内图片可恢复） |

命令行快速自检：
```bash
curl -s http://127.0.0.1:3000/socket.io/?EIO=4\&transport=polling | head -c 100
# 预期返回 0{"sid":...
curl -s -X POST http://127.0.0.1:3000/peerjs/peerjs/id\?ts=$(date +%s)
# 预期返回一个 UUID
```

---

## 🐛 宝塔环境常见问题

### 1. 网页能打开，但进房间后没有视频/消息
**原因**：反向代理缺少 WebSocket 升级头。
**解决**：按第四步第 3 节检查 `Upgrade` / `Connection` 两行；改完宝塔会自动 reload。

### 2. 连接 1 分钟左右自动断开
**原因**：`proxy_read_timeout` 默认 60s，静默连接被 Nginx 掐断。
**解决**：`proxy_read_timeout 300s;`（客户端本身有心跳，300s 足够）。

### 3. 手机浏览器看不到自己的画面
**原因**：非 HTTPS 环境浏览器禁用摄像头。
**解决**：完成第五步 SSL + 强制 HTTPS，用 `https://` 访问。

### 4. 两边都连上了但互相看不到画面
**原因**：P2P 打洞失败（双方都在对称型 NAT 后面，如部分企业网/4G）。
**说明**：信令正常时聊天和文件仍可用；视频需要 STUN/TURN。可在 `client/js/i.js` 的 PeerJS
配置中增加 `config: { iceServers: [...] }`，加入自己的 TURN 服务器。

### 5. 502 Bad Gateway
**原因**：Node 进程没起来或挂了。
**解决**：`pm2 status && pm2 logs webrtc-app --lines 50`；确认 3000 端口在监听：
`netstat -tlnp | grep 3000`。

### 6. 管理员面板登录一直失败
**原因**：管理员 IP 白名单默认只允许本机（见第六步）。
**解决**：把你的公网 IP 加入 `ADMIN_CONFIG.allowedIPs` 或改为 `["*"]`，然后 `pm2 restart`。

### 7. 与同服务器其他网站互相影响吗
不影响：本项目只有一个 3000 内网端口 + 一个站点反代；P2P 媒体流不经过服务器，
CPU/带宽占用与普通小型 Node 网站相当。数据都写在项目 `data/` 目录，不碰其他站点。

---

## 💾 数据备份（可选）

宝塔 → **计划任务** → 添加备份任务：
```bash
tar -czf /www/backup/webrtc-data_$(date +\%Y\%m\%d).tar.gz -C /www/wwwroot/webrtc-video-chat data
```
保留最近 N 份，防止 JSON 数据文件损坏丢失。

---

## 🔗 相关文档

- [真实域名部署指南.md](真实域名部署指南.md) — 通用域名部署（非宝塔）
- [快速域名测试指南.md](快速域名测试指南.md) — 快速验证流程
- [README.md](README.md) — 功能总览

---

**最后更新**：2026年9月13日
